import Ping from 'domain/ping/Ping';

export default class PingApplicationService {

	getPing(name : string) : Ping  {
		return Ping.create(name).duplicate();
	}

	static create() : PingApplicationService {
		return new PingApplicationService();
	}
}
